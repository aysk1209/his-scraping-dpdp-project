"""CLI entry point for generating a synthetic HIS dataset.

Stub — wired up in build step 3. Will emit records for each of the five HIS
layers (see `interop.layers`) into a gitignored output directory, optionally
shaped into HL7/FHIR structures.
"""

from __future__ import annotations


def main() -> None:
    raise SystemExit("Not implemented yet — see build step 3 in CLAUDE.md.")


if __name__ == "__main__":
    main()
