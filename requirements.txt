# Runtime dependencies for the HIS scraping + DPDP compliance benchmarking project.
# Pinned to current major versions. Confirm with the team before adding anything here
# (CLAUDE.md: "Confirm before introducing a new major dependency").

# --- core data handling ---
pandas>=2.2,<3.0

# --- schema modelling: DPDP compliance rules + five-layer HIS record schemas ---
pydantic>=2.9,<3.0

# --- synthetic HIS data generation ---
Faker>=30.0,<40.0

# --- testing ---
pytest>=8.0,<9.0

# --- mock HIS portal (test fixture, tools/mock_portal) ---
# A login-gated, server-rendered portal over a HISDataSource so the Tier 2
# browser layer has something real to scrape. Team sign-off 2026-09-12.
flask>=3.0,<4.0

# --- Tier 2 headless browser automation ---
# Installed now so the extraction skeleton can be written; exercised only once
# credentialed live-HIS access is usable (build step 7, currently blocked).
playwright>=1.47,<2.0

# --- AI-agent comparison (guide's request, 2026-09-16) ---
# The benchmark compares our technique against publicly available AI agents
# (extraction/techniques/ai_agent.py). Each provider's official SDK is imported
# lazily and needed only to *record* decisions live (scripts/record_ai_agents.py);
# the demos and tests replay committed recordings and run without any of these.
anthropic>=0.125
openai>=1.0
google-genai>=1.0

# --- review deck builder (tools/build_review_deck.py) ---
# Fills the mandatory VIT/SENSE template from the benchmark artefacts. Tooling,
# not pipeline: nothing under src/ imports it.
python-pptx>=1.0,<2.0

# ------------------------------------------------------------------
# Deferred, pending team sign-off:
#   ruff / black   - lint + format
#   pytest-cov     - coverage reporting for the benchmark evidence trail
#   mypy           - static type checking
# ------------------------------------------------------------------
